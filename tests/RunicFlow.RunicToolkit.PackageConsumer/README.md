# Runic Flow / Runic Toolkit package consumer

This gate restores the committed NativeAOT smoke project from locally packed
`RunicFlow` packages and the exact published `RunicToolkit.Desktop` package. It
then publishes and executes the isolated consumer without project references.
