# Application Bridge package consumer

This isolated fixture restores the locally packed `RunicFlow` and
`RunicFlow.ApplicationBridge` artifacts alongside the exact published Application
Bridge dependency, then publishes and executes the shared NativeAOT smoke test.
